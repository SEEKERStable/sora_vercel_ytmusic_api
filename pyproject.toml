[project]
name = "sora-ytmusic-api"
version = "1.0.0"
description = "SORA YouTube Music search API for Vercel"
requires-python = ">=3.12"
dependencies = [
    "fastapi>=0.115.0",
    "ytmusicapi>=1.12.1"
]

[tool.vercel]
entrypoint = "app:app"